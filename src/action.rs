use serde_derive::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub enum Action {
  Tick,
  Error(String),
  Quit,
  Refresh,
  GotoBottom,
  GotoTop,
  GotoPageBottom,
  GotoPageTop,
  Down,
  Up,
  PageDown,
  PageUp,
  Delete,
  Done,
  ToggleStartStop,
  ToggleMark,
  ToggleMarkAll,
  QuickTag,
  Select,
  SelectAll,
  Undo,
  Edit,
  Shell,
  Help,
  ToggleZoom,
  Context,
  Next,
  Previous,
  Shortcut(usize),
  Modify,
  Log,
  Annotate,
  Filter,
  Add,
}
